[project]
name = "enfolded-orichalcum"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
test = [
    "pytest>=8",
    "pytest-playwright>=0.5",
    "lxml>=5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
